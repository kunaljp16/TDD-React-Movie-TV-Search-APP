// import { createStore } from "redux";

// const headerReducer = (state = { toggleNavbarMenuState: true }, action) => {
//   if (action.type === "toggleMenubar") {
//     return {
//       toggleNavbarMenuState: false,
//     };
//   }

//   return state;
// };

// const store = createStore(headerReducer);

// export default store;
